{
  "name": "blackboxfem",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:jest": "jest",
    "push:Git": "git add . && git commit -m 'Configuracion del entorno de jest' && git push",
    "install:jest": "install && install --save-dev ts-jest && install --save-dev ts-jest && install --save-dev @types/node && install && install axios --save-dev && npm install --save-dev ts-node"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.4",
    "axios": "^1.6.8",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.2.2",
    "vite": "^5.1.4"
  }
}
